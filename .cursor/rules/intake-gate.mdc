---
description: rockit 啟動門檻：資訊不足時硬性阻擋，不得跑任何工具
globs: ["**/*"]
alwaysApply: true
---

# 啟動門檻（Hard Gate）

診斷流程開始前必須通過以下檢查。**任何一項不通過就停下來，列出缺什麼以及怎麼取得，
不要跑任何工具、不要猜測、不要「先做再說」。**

## Gate 0：rockit 該不該介入

至少要有下列其中一項才啟動診斷流程：

- 具體症狀描述（哪個 API/操作卡住、觀察到什麼現象）
- log 內容（dmesg / rocgdb / fence_info / collector 輸出任一）
- 已存在的 bundle 或 log 檔路徑
- 明確引用某個既有 case

只說「GPU 有問題」「跑不動」而沒有任何上述資訊時，不要進入診斷流程，
直接問：發生在哪個階段、有什麼錯誤訊息、有沒有 log。

## Gate 1：執行任何工具前

必須先確認以下兩項，缺一不可：

| 項目 | 為什麼必要 |
|------|-----------|
| **執行模式**：本機（agent 可直接跑）或 客戶機（給命令等貼回） | 決定 agent 能不能自己執行；客戶機模式下 agent 不可假設有存取權 |
| **目標識別**：PID、process name 或 container 名稱 | 多數工具需要 `--pid`；沒有目標就抓不到 KFD queue metadata |

缺這兩項時，先問清楚再動作。不要用 `all` 或自動偵測繞過，
那會在多 process 環境抓錯目標。

## Gate 2：宣稱「這是 hang」之前

不要只根據 GPU util 100% 就判定 hang。至少確認：

- process / container 仍存活，且 PID 沒有變化
- workload 之前曾正常輸出業務進度指標
- 最近約 60 秒沒有新的進度輸出
- 至少一張目標 GPU 為 100%
- GPU VRAM 仍被目標 process 佔用
- 不是所有目標 GPU 都已降為 0%

完整判定條件見 `knowledge/driver/playbooks/hang_collection_sop.md` 第 6 節。

條件不足時可以說「症狀疑似 hang，但尚未滿足判定條件」，
**不要**直接當作已確認的 hang 往下跑完整採集。

## Gate 3：進入各 Phase 前

每個 phase 有自己的前置條件，見 `.cursor/skills/diagnose/SKILL.md` 的 phase 契約表。
不滿足時明確告知停在哪個 phase、還缺什麼才能繼續。

## 阻擋時的回應格式

```
目前無法進入 <phase 名稱>，缺少：

- <缺少的項目 1>：<怎麼取得，附命令>
- <缺少的項目 2>：<怎麼取得，附命令>

補齊後可以直接從 <phase> 開始，不需要重跑前面的步驟。
```

最後一句很重要：讓使用者知道補完資料後可以從中間接續，不用從頭來過。
