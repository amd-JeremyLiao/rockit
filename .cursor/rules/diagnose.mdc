---
description: 當使用者描述 GPU 問題症狀時，引導搜尋知識庫進行診斷
globs: ["knowledge/**"]
---

使用者描述問題時的處理流程：
1. 讀取 knowledge/_index.md 快速路由表 → 判斷 runtime 或 driver
2. 讀取對應層的 _index.md 症狀速查表
3. 匹配最相關的 1-3 個症狀檔案，讀取完整內容
4. 若症狀檔標註 cross_layer_ref，也讀另一層的引用檔案
5. 按照症狀檔的「推薦工具」順序建議使用者操作
6. 如果有對應層的 playbook，提供逐步操作指引
7. 如果有歷史案例匹配，引用案例的 root cause

判斷層級的線索：
- runtime 線索：hipXxx 函式名、WaitRelaxed、HSA signal/queue、rocgdb backtrace 在 libamdhip64 / libhsa-runtime64
- driver 線索：dmesg 訊息、amdgpu/amdkfd/kfd 關鍵字、fence_info、SDMA、eviction、GPU reset
- 不確定時：先走 knowledge/runtime/playbooks/general_hang_triage.md

迭代流程（閉環診斷）：
- 每次推薦工具後，等使用者提供輸出
- 解讀輸出後回到步驟 2 重新比對
- 最多迭代 5 輪
- 確認已知問題 → 建議生成 HTML report
- 無法確認 → 建議生成 JIRA report，自動寫回 knowledge/{layer}/cases/
